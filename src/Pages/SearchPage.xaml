<Page x:Class="MusicCatalog.Pages.SearchPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:MusicCatalog.Pages"
      xmlns:fluentWpf="clr-namespace:SourceChord.FluentWPF;assembly=FluentWPF"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Search Results">
    <Grid>
        <StackPanel Orientation="Horizontal" Margin="10,40,0,0">
            <TextBlock Text="Search Results for '" FontSize="18"></TextBlock>
            <TextBlock x:Name="TextBlockSearchTerm" 
                       FontSize="18"
                       Text="{Binding SearchText}"></TextBlock>
            <TextBlock Text="' >"
                       FontSize="18"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
